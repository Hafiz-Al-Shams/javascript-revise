// && -> and
// || -> or
// ! -> not/reverse -> exclamation(!)

let isLoggedIn = true;
let isPaidUser = false;

// AND operator
console.log(isLoggedIn && isPaidUser); // false

let isEmailUser = true;
let isGoogleUser = false;

console.log(isEmailUser || isGoogleUser); // true